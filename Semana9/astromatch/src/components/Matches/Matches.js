import React from 'react'

function Matches (props){
    return (
        <div>
            <button onClick={props.onClickMatches}>Pretenders</button>
            Matches            
        </div>
    )
}

export default Matches